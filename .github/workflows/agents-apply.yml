name: agents-apply

on:
  pull_request:
    types: [labeled]

permissions:
  contents: write
  pull-requests: write

jobs:
  apply:
    if: contains(github.event.pull_request.labels.*.name, 'agents:apply')
    uses: Mindgames/changelog-agent/.github/workflows/agents-codex.yml@main
    secrets: inherit
    with:
      mode: apply
      always_include: "xyz"
      model: gpt-5-codex
