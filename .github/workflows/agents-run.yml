name: agents

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read

jobs:
  agents-updater:
    uses: ./.github/workflows/agents-codex.yml
    with:
      mode: suggest
      always_include: "xyz"
